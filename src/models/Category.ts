export default class Category {
    ID: string;
    title: string;
    color: string;

    constructor(ID: string, title: string, color: string) {
        this.ID = ID;
        this.title = title;
        this.color = color;
    }
}
